# Avenger V4.0 — 全栈开发者全景工作台

> 本地离线运行的一站式开发环境管控平台
> Apple iOS 26 / visionOS Liquid Glass 液态玻璃 · 粒子系统（CDN + 离线 canvas 双引擎）· 全 SVG 几何图标
> Windows 10/11 · Python 3.8+ · 零第三方 Python 依赖

---

## 快速开始

双击 `一键启动Avenger.bat`，自动启动服务并打开浏览器。
默认地址 `http://127.0.0.1:8765`，端口占用自动顺延。
**关掉浏览器没关系**：右下角「Avenger 托管」小窗继续保活服务；**关掉小窗才会停止**。

---

## 文件清单

| 文件 | 说明 |
|------|------|
| `avenger_server.py` | 后端主程序（纯标准库：并行扫描引擎 + 诊断引擎 + 硬件探测 + REST API + AI 流式代理） |
| `avenger_studio.py` | 工作室模块：备忘录(SQLite)、AI 接入(18 家供应商+流式)、练习题库(26 题)、速查(18 篇) |
| `avenger_hud.py` | 托管小窗（tkinter）：保活服务 + 实时 CPU/内存/环境数 + 宠物同步 |
| `avenger.html` | 前端单文件（内嵌 CSS/JS，液态玻璃系统 + 16 大模块） |
| `一键启动Avenger.bat` | 双击启动，自动检测 Python→启动服务→打开浏览器 |
| `PythonEnvManager.bat` | 全功能命令行兜底脚本（纯原生 BAT） |
| `PRODUCT.md` | 产品定义文档 |
| `使用说明.md` | 本文档 |

---

## V4.0 跨越式升级亮点

### 性能：总驾驶舱秒开
- **粒子脚本不再阻塞首屏**：tsParticles 改为页面渲染后动态加载；离线/加载失败自动切换内置 canvas 粒子引擎，无网络也有效果
- **驾驶舱快照缓存**：再次打开时用上次数据瞬时渲染（带「快照」角标），后台并行刷新覆盖
- **并行环境扫描**：版本/pip/包数检测多线程并行（约 3-6 倍提速），包计数改用 `importlib.metadata`（比 pip list 快约 10 倍）
- **定点更新**：星标环境不再整页重绘；多语言检测先出缓存再补刷，不再等待
- 日志自动轮转（512KB 裁剪）、任务句柄 30 分钟自动回收

### AI 工坊（全面升级）
- **流式输出**：打字机效果，可随时「停止」；不支持流式时自动回退整段返回
- **18 家 OpenAI 兼容供应商**：Ollama / LM Studio（本地免 Key）、OpenAI、Claude 网关、OpenRouter、DeepSeek、硅基流动、通义千问、智谱 GLM、Kimi、Gemini、Groq、xAI、Cerebras、Together、Mistral、豆包方舟等
- **6 种专家角色**：默认 / 代码专家 / 架构顾问 / 讲解老师 / 面试官 / 中英互译（系统提示词注入）
- 温度滑杆、对话历史本地保存、Markdown/代码块渲染、一键复制、测试连接（含时延）

### 编程练习场（26 题）
- 难度筛选（入门/简单/中等/困难）+ 主题筛选 + 搜索
- **进度环**（已通过 x/26）、**连胜天数**（每天至少通过 1 题）、今日挑战
- 代码草稿自动保存、重置代码、随机一题、运行计时、通过撒花
- 本地 Python isolated 判题，8 秒超时，测试用例服务端持有

### 开发者备忘录
- SQLite 本地存储（avenger_notes.db），不经过网络
- 搜索、标签筛选、置顶、**Markdown 预览**（工具栏切换）、Ctrl+S 快速保存
- 字数统计、更新时间、**JSON 导入/导出**

### 速查学习（18 篇）
- Git / pip / 正则 / HTTP / SQL / 窗口函数 / 常用命令 / Python 调试 / curl / Docker / PowerShell / Python 惯用法 / asyncio / JS ES2023 / CSS 布局 / 正则小抄本 / Vim / Node npm
- 关键词搜索 + 悬停一键复制全文

### 皮肤与宠物（自定义）
- **12 套皮肤**：余烬 / 暮紫 / 森野 / 墨蓝 / 蔷薇 / 终端 / 鎏金 / 极光 / 樱粉 / 深海 / 赛博 / 石墨 —— 每套带专属氛围光斑
- 自定义强调色取色器（与预设叠加）
- **10 只桌面宠物**：Ember / Pip / Nora / Orb / Bit / Foxy / Ghost / Zap / Pengu / Pixel
- 宠物可**拖拽定位**（记忆位置）、点击互动带**气泡台词**、可隐藏
- 皮肤与宠物自动同步到托管小窗（重启小窗生效）

### 视觉与体验
- 五层液态玻璃材质 + **三档玻璃质感**（全效 / 均衡 / 性能优先，低配机不再卡顿）
- 粒子密度四档可调（关闭/低/中/高），帧率 <50fps 自动降级
- 弹簧物理动效 `cubic-bezier(0.32,0.72,0,1)`、滚动揭示、微交互光点
- 深浅双主题自动跟随系统、护眼暖色模式 + 定时开关
- 侧边栏 16 模块分组导航，可折叠

### 十六大功能模块
| 模块 | 说明 |
|------|------|
| **总览驾驶舱** | 快照秒开 + 环境统计 + 硬件实时图表 + 运行时检测 + 扫描进度条 + 最近操作 |
| **Python 环境** | 系统 Python 管理（拖拽排序 PATH）、虚拟环境、PIP 缓存、诊断修复 |
| **多语言运行时** | Node / Go / Java / Rust / PHP / Ruby / .NET / Docker / Git 检测与下载指引 |
| **包管理** | 虚拟滚动、版本切换、依赖树/力导向图、批量升级、跨环境搜索、按需孤儿检测 |
| **AI 工坊** | 流式对话、多供应商、角色系统、历史记录、测试连接 |
| **开发者备忘录** | SQLite 笔记、MD 预览、标签、导入导出 |
| **练习场** | 26 题 kata、进度/连胜/每日挑战、本地判题 |
| **速查学习** | 18 篇速查、搜索、复制 |
| **开发者工具箱** | JSON、正则、编码、哈希、密码、时间戳、颜色、JWT、UUID、Cron、SQL、HTML→JSX、diff、本机 HTTP、片段、进制、URL |
| **数据库管理** | MySQL / PostgreSQL / Redis 端口探测 + SQLite 只读浏览 |
| **Git 辅助** | 本地仓库扫描、分支与提交概览 |
| **端口与进程** | 端口占用列表、一键终止 |
| **硬件监控** | CPU / 内存 / GPU(nvidia-smi) / 磁盘 / 电池，实时图表 + 磁盘树图下钻 |
| **编程健康** | 20-20-20 护眼、久坐休息、饮水提醒、编程时长统计 |
| **插件市场** | 核心模块 + 可开关入口 + 声明式插件 API |
| **设置** | 皮肤画廊、宠物网格、粒子/玻璃性能档、主题、护眼定时、数据管理 |

### Python 核心能力（V2.x 全量保留并增强）
- 健康度评分、拖拽排序 PATH、虚拟滚动、依赖树可视化
- 精准版本切换、深度诊断（循环依赖/过时包/孤儿包/残留文件）
- 诊断报告导出、环境克隆、多环境对比（2-3个）
- 缓存精细化管理、一键回滚、Ctrl+K 命令面板（支持 > scan 等斜杠命令）
- 环境快照、快照对比、依赖安全扫描（OSV.dev + 本地规则，离线降级）

### 安全
- 所有修改操作二次弹窗确认；危险操作需输入确认词
- 修复前自动导出 requirements.txt 到 backups/；支持撤销（Ctrl+Z）
- 包名严格校验（防 pip 参数注入）、缓存清理白名单路径校验、残留删除防路径越界
- 请求体解析加固：非法编码/格式优雅忽略，不再断连
- 仅监听 127.0.0.1，会话令牌 + Host/Origin 校验（防 DNS rebinding），零数据收集

---

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl+K / Ctrl+P | 命令面板 / 全局搜索 |
| Ctrl+H | 返回总览驾驶舱 |
| Ctrl+1~9 / Ctrl+0 | 切换模块 |
| Ctrl+R | 重新扫描环境 |
| Ctrl+D | 切换明暗主题 |
| Ctrl+Z | 撤销上次危险操作 |
| Ctrl+S | 备忘录页快速保存 |
| Esc | 关闭弹窗/引导 |
| ↑↓ | 命令面板导航 |

---

## 技术说明

- **液态玻璃**：CSS `backdrop-filter` 五层材质规范；三档质感可降级
- **粒子**：tsParticles 2.12（CDN 动态加载，离线自动切换内置 canvas 引擎）
- **AI 流式**：后端解析供应商 SSE 并转发为纯文本流（urllib 分块读 + Connection: close），前端 ReadableStream 渲染
- **硬件监控**：ctypes GetSystemTimes/GlobalMemoryStatusEx + PowerShell CIM 缓存刷新 + nvidia-smi
- **零第三方 Python 依赖**，全部使用标准库

---

## 常见问题

**Q: 驾驶舱打开慢？**
A: V4.0 已重构：粒子脚本不再阻塞、快照秒开、扫描并行提速。若首次启动仍慢，等后台扫描完成后即恢复秒开。

**Q: 启动提示未检测到 Python？**
A: 安装 Python 3.8+ 并勾选 Add Python to PATH。

**Q: AI 流式没反应？**
A: 先点「测试连接」。本地模型确认 Ollama/LM Studio 已启动；云端需保存 Key。个别供应商不支持流式时自动回退整段返回。

**Q: 宠物在托管小窗里没变？**
A: 小窗在启动时读取宠物设置，更换后重启小窗即可同步。

**Q: 如何回滚？**
A: Python 模块→诊断修复子页→「备份回滚」，选择备份文件一键恢复；或 Ctrl+Z 撤销最近操作。
